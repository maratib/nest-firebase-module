{
  "name": "nest-firebase-module",
  "version": "0.0.1",
  "description": "Firebase, NestJS, ",
  "author": "Maratib Ali Khan",
  "license": "MIT",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "@nestjs/common": "^7.6.5",
    "firebase-admin": "^9.4.2",
    "rxjs": "^6.6.3"
  },
  "devDependencies": {
    "@types/node": "^14.14.20",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  }
}
